apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  labels:
    app: {{ .Chart.Name }}
data:
  mcp-servers.json: |
    {{- .Values.mcpConfig | toPrettyJson | nindent 4 }}